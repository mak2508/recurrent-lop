# Model Configuration
model:
  model_type: "MLP"
  input_size: 784
  hidden_size: 25
  num_classes: 10

# Training Configuration
training:
  algo: "BP"
  num_epochs: 30
  batch_size: 6000
  learning_rate: 0.01
  num_tasks: 50
  to_perturb: False

exp_desc: "mlp"
